meta:
target=gml_Object_obj_boulder_Alarm_0
critical=true
patch:

/*
WARNING! SHITTY FORMAT BELOW.

// Tis statue dialogue
// TARGET: TAIL
else if (b_form == 10) {
	text[0] = "Don't lose my way..."
    voice = b_voice
    moods = [neutral]
    speakers = [id]
}
*/